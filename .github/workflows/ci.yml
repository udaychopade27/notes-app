name: CI-DevSecOps

on:
  push:
    branches:
      - main

env:
  IMAGE_TAG: ${{ github.run_number }}

jobs:

  security-and-build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      # -----------------------------------
      # Secret Scan
      # -----------------------------------
      - name: Gitleaks Scan
        uses: gitleaks/gitleaks-action@v2

      # -----------------------------------
      # Filesystem Scan
      # -----------------------------------
      - name: Trivy Filesystem Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .

      # -----------------------------------
      # Docker Login
      # -----------------------------------
      - name: DockerHub Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # -----------------------------------
      # Build Backend
      # -----------------------------------
      - name: Build Backend Image
        run: |
          docker build \
          -t ${{ secrets.DOCKERHUB_USERNAME }}/notes-app-backend:${IMAGE_TAG} \
          ./backend

      # -----------------------------------
      # Build Frontend
      # -----------------------------------
      - name: Build Frontend Image
        run: |
          docker build \
          -t ${{ secrets.DOCKERHUB_USERNAME }}/notes-app-frontend:${IMAGE_TAG} \
          ./frontend

      # -----------------------------------
      # Scan Backend Image
      # -----------------------------------
      - name: Scan Backend Image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/notes-app-backend:${IMAGE_TAG}

      # -----------------------------------
      # Scan Frontend Image
      # -----------------------------------
      - name: Scan Frontend Image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/notes-app-frontend:${IMAGE_TAG}

      # -----------------------------------
      # Push Images
      # -----------------------------------
      - name: Push Backend Image
        run: |
          docker push \
          ${{ secrets.DOCKERHUB_USERNAME }}/notes-app-backend:${IMAGE_TAG}

      - name: Push Frontend Image
        run: |
          docker push \
          ${{ secrets.DOCKERHUB_USERNAME }}/notes-app-frontend:${IMAGE_TAG}